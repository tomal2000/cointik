name: Deploy

on:
  push:
    branches: [ main ]
jobs:
  Make:
    runs-on: ubuntu-latest
    name: Make ${{matrix.work}} ${{matrix.arch}}
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: down
        uses: MitchWind/ftp-deploy@master
        with:
            server: "ftp.itlab.com.bd"
            username: "cointik@itlab.com.bd"
            password: ${{ secrets.FTP_PASSWOD }}
